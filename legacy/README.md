Legacy artefacts from the initial 2025 capstone submission.

These files are kept for provenance and comparison. The current implementation lives under `src/`
and is being refactored into a reusable, production-style pipeline.
